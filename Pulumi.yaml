name: kubernetes-aws-python
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a Kubernetes cluster on AWS based on python programming language
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-aws-python
